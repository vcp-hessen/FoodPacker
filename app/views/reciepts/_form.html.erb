<%= form_for(@reciept) do |f| %>
  <% if @reciept.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@reciept.errors.count, "error") %> prohibited this reciept from being saved:</h2>

      <ul>
      <% @reciept.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
