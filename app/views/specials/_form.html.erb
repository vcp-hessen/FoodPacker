
  <div class="group">
    <%= f.label :group_id, t("activerecord.attributes.special.group_id", :default => "Group"), :class => :label %>
    <%= f.collection_select :group_id, Group.all, :id, :name %>
    <span class="description">Ex: a simple text</span>
  </div>

  <div class="group">
    <%= f.label :box_id, t("activerecord.attributes.special.box_id", :default => "Box"), :class => :label %>
    <%= f.collection_select :box_id, Box.all ,:id, :name%>
    <span class="description">Ex: a simple text</span>
  </div>

  <div class="group">
    <%= f.label :product_id, t("activerecord.attributes.special.product_id", :default => "Product"), :class => :label %>
    <%= f.collection_select :product_id, Product.all, :id, :name_with_unit %>
    <span class="description">Ex: a simple text</span>
  </div>

  <div class="group">
    <%= f.label :quantity, t("activerecord.attributes.special.quantity", :default => "Quantity"), :class => :label %>
    <%= f.text_field :quantity, :class => 'text_field' %>
    <span class="description">Ex: a simple text</span>
  </div>
        
<div class="group navform wat-cf">
  <button class="button" type="submit">
    <%= image_tag("web-app-theme/icons/tick.png", :alt => "#{t("web-app-theme.save", :default => "Save")}") %> <%= t("web-app-theme.save", :default => "Save") %>
  </button>
  <span class="text_button_padding"><%= t("web-app-theme.or", :default => "or") %></span>
	<%= link_to t("web-app-theme.cancel", :default => "Cancel"), specials_path, :class => "text_button_padding link_button" %>
</div>